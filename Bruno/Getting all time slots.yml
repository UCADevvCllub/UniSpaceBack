info:
  name: Getting all time slots
  type: http
  seq: 13

http:
  method: GET
  url: "{{host}}/api/schedule-entries/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
