info:
  name: Populating subject event
  type: http
  seq: 33

http:
  method: POST
  url: http://127.0.0.1:8000/api/subject-entries/
  body:
    type: json
    data: |
      {
        "name": "MATH"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
