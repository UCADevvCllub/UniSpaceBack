info:
  name: Populating subject event
  type: http
  seq: 25

http:
  method: POST
  url: "{{host}}/api/subject-entries/"
  body:
    type: json
    data: |
      {
        "name": "MATH"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
