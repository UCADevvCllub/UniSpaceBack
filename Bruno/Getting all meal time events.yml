info:
  name: Getting all meal time events
  type: http
  seq: 11

http:
  method: GET
  url: "{{host}}/api/meal-times/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
