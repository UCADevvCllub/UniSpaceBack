info:
  name: Filtering class event by course
  type: http
  seq: 15

http:
  method: GET
  url: "{{host}}/api/schedule/?course=cs"
  params:
    - name: course
      value: cs
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
