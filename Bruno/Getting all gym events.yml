info:
  name: Getting all gym events
  type: http
  seq: 10

http:
  method: GET
  url: "{{host}}/api/gym-events/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
