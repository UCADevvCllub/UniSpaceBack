info:
  name: Populating instructor event
  type: http
  seq: 36

http:
  method: POST
  url: "{{host}}/api/instructor-entries/"
  body:
    type: json
    data: |
      {
        "first_name": "Jane",
        "last_name": "Smith",
        "status": "ON_CAMPUS"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
