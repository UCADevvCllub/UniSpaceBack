info:
  name: Getting all events
  type: http
  seq: 30

http:
  method: GET
  url: "{{host}}/api/schedule-entries/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
