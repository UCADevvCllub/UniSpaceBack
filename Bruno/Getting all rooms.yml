info:
  name: Getting all rooms
  type: http
  seq: 18

http:
  method: GET
  url: "{{host}}/api/room-entries/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
