info:
  name: Getting all subjects
  type: http
  seq: 22

http:
  method: GET
  url: "{{host}}/api/subject-entries/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
