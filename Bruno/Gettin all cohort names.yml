info:
  name: Gettin all cohort names
  type: http
  seq: 26

http:
  method: GET
  url: "{{host}}/api/cohort-entries/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
