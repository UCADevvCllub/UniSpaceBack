info:
  name: Gettin all cohort names
  type: http
  seq: 26

http:
  method: GET
  url: http://127.0.0.1:8000/api/cohort-entries/
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
