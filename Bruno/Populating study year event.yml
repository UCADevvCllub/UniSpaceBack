info:
  name: Populating study year event
  type: http
  seq: 45

http:
  method: POST
  url: http://127.0.0.1:8000/api/study-year-entries/
  body:
    type: json
    data: |
      {
        "year_name": "FESH"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
