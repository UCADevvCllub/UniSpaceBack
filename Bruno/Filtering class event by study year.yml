info:
  name: Filtering class event by study year
  type: http
  seq: 16

http:
  method: GET
  url: "{{host}}/api/schedule/?study_year=2029"
  params:
    - name: study_year
      value: "2029"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
