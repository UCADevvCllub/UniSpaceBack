info:
  name: Getting all class events
  type: http
  seq: 12

http:
  method: GET
  url: "{{host}}/api/schedule/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
