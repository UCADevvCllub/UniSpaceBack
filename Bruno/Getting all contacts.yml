info:
  name: Getting all contacts
  type: http
  seq: 17

http:
  method: GET
  url: "{{host}}/api/contacts/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
