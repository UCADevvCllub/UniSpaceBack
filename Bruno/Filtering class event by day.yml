info:
  name: Filtering class event by day
  type: http
  seq: 14

http:
  method: GET
  url: "{{host}}/api/schedule/?day=FRI"
  params:
    - name: day
      value: FRI
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
