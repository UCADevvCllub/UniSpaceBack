info:
  name: Getting all instructor names
  type: http
  seq: 24

http:
  method: GET
  url: http://127.0.0.1:8000/api/instructor-entries/
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
