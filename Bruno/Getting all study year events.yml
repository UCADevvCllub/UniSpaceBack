info:
  name: Getting all study year events
  type: http
  seq: 28

http:
  method: GET
  url: "{{host}}/api/study-year-entries/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
